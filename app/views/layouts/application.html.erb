<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>FullStackChallenge</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'shared/navigation_bar' %>
<div id="main-container" class="container-fluid">
  <div class="row">
    <%= render 'eligibilities_list/eligibility_list' %>
    <div id="main" class="col-sm-9 col-md-10 main">
      <div class="flashes">
        <% if flash[:success] %>
            <p class="success"><%= flash[:success] %></p>
        <% end %>
        <% if flash[:alert] %>
            <p class="alert"><%= alert %></p>
        <% end %>
      </div>
      <%= yield %>
    </div>
  </div>
</div>
<!--
There is a weird behavior for a particular size, adding 30px padding for it
-->
<script>
    $(window).on('load resize', function() {
        $('body').css({"padding-top": $(".navbar").height() + 30 + "px"});
    });
</script>
</body>
</html>
