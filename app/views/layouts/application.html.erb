<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>FullStackChallenge</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'shared/navigation_bar' %>
<div id="main-container" class="container-fluid">
  <div class="row">
    <div class="container">
      <% if flash[:success] %>
          <p class="alert alert-success alert-dismissable"><%= flash[:success] %></p>
      <% end %>
      <% if flash[:alert] %>
          <p class="alert alert-danger alert-dismissable"><%= alert %></p>
      <% end %>
    </div>
    <%# Restrict the side bar to the index page while viewing the set of resoruce sites only %>
    <% if params[:controller] == 'resource_sites' && params[:action] == 'index'%>
        <%= render 'shared/resource_sites_sidebar' %>
    <% end %>
    <%= yield %>
  </div>
</div>
</body>
</html>
