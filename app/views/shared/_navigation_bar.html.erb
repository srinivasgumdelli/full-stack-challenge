<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-brand-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Healthify</a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-brand-collapse">
      <ul class="nav navbar-nav">
        <li><a href="<%= resource_sites_path %>">Resource Sites<span class="sr-only">(current)</span></a></li>
        <% if user_signed_in? && current_user.admin? %>
            <li><a href="<%= eligibilities_path %>">Eligibilities<span class="sr-only">(current)</span></a></li>
        <%end%>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="<%= new_resource_site_path %>">New Resource Site</a></li>
        <% if user_signed_in? && current_user.admin? %>
            <li><%= link_to('Add New Eligibility', new_eligibility_path) %></li>
        <%end%>
        <% if user_signed_in? %>
            <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
        <% else %>
            <li><%= link_to('Login', new_user_session_path) %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>